services:
  backend:
    build:
      context: ./atlas-data
      dockerfile: Dockerfile
    env_file:
      - ./atlas-data/.env
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    depends_on:
      - backend
    ports:
      - "8080:80"
    restart: unless-stopped
